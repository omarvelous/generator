<% if resource.errors.any? %>
  <div id="error_explanation">
    <h2 class="alert-heading"><%= pluralize(resource.errors.count, "error") %> found:</h2>
    <ul>
    <% resource.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  </div>
<% end %>
